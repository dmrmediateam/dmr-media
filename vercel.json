{
  "installCommand": "dnf install -y git-lfs && git lfs install && git lfs pull && npm install",
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_SANITY_PROJECT_ID": "cs56ipyy",
    "NEXT_PUBLIC_SANITY_DATASET": "production"
  }
}
